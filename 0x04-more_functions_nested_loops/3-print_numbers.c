#include "main.h"

/**
 * print_numbers - this is where the numbers will be printed
 * Return: returns 0
 */

void print_numbers(void)
{
	int i
		;
	for (i >= 0; i <= 9; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
