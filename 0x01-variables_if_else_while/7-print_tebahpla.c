#include <stdio.h>

/**
 * main - where the code starts
 *
 * Description - the main code ges here
 *
 * Return: return 0
 */
int main(void)
{
	int ch;

	for (ch = 'z'; ch <= 'a'; ch--)
	{
		putchar(ch);
	}

	putchar('\n');
	return (0);
}
