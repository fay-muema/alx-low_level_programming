#include <stdio.h>

/**
 * main - where the code starts
 *
 * Description - where the code is written
 *
 * Return: returns 0
 */

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	return (0);
}
